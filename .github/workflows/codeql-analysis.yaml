name: "CodeQL"

permissions: {}

on:
  push:
    branches: [ '*' ]
  schedule:
    - cron: '44 0 * * 5'

jobs:
  analyze:
    name: CodeQL analysis for ${{ inputs.language }}
    permissions:
      actions: read  # Required to run CodeQL analysis
      contents: read  # Required to access the repository's code
      security-events: write  # Required to upload the analysis results to GitHub
    strategy:
      fail-fast: false
      matrix:
        language: [ 'python' ]
    uses: UBC-MOAD/gha-workflows/.github/workflows/codeql-analysis.yaml@main  # zizmor: ignore[unpinned-uses]
    with:
      language: ${{ matrix.language }}
